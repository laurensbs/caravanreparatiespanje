/** Stable error code — clients map this to `scheduleNeedsTasks` in planning-locale. */
export const SCHEDULE_NEEDS_TASKS = "SCHEDULE_NEEDS_TASKS";

/** Admin repair-detail (English, same tone as other repair toasts). */
export const SCHEDULE_NEEDS_TASKS_ADMIN_TOAST =
  "Add at least one task on this work order before you can put it on the planning calendar or in the workshop.";
